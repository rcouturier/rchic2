# R-portable for Windows

Placez ici les binaires R compilés pour Windows.

## Structure attendue

```
R-portable-win/
├── bin/
│   └── Rscript.exe (binaire Windows - à fournir)
└── bin/x64/
    ├── Rscript.exe
    └── R.exe
```

## Notes

- Le fichier `bin/Rscript.exe` dans ce dossier est un placeholder
- Remplacez-le avec le vrai binaire R pour Windows
- Les binaires x64 sont préférés si disponibles
